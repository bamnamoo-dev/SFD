# 📊 예산관리 프로그램 (학교회계 통합 대시보드)

K-에듀파인 데이터를 기반으로 학교회계 예산 집행 현황을 모니터링하고, 추경 예산을 실시간으로 시뮬레이션 및 관리할 수 있는 모던 웹 대시보드 애플리케이션입니다.

---

## 🌐 전체 프로젝트 생태계 구성

| 구분 | 프로젝트 경로 | 설명 |
| :--- | :--- | :--- |
| **메인 포털** | `G:\내 드라이브\antigravity\asisen-store` | 전체 통합 메인 페이지 / 스토어 포털 |
| **챗봇 서비스** | `G:\내 드라이브\antigravity\sen-chatbot-v2` | AI 기반 학교 행정/예산 지원 챗봇 |
| **예산정산 프로그램** | `G:\내 드라이브\antigravity\예산정산프로그램` | 예산 지출 및 정산 내역 관리/대사 프로그램 |
| **예산관리 프로그램** *(현재)* | `G:\내 드라이브\antigravity\예산관리프로그램` | K-에듀파인 연동 예산 집행 대시보드 & 실시간 추경 시뮬레이터 |

---

## 🚀 주요 기능 및 고도화 내역 (v2.5 Pro)

1. **상단 4대 핵심 재정 지표 (KPI Cards)**
   - `총 예산현액 (A)`: 기정 예산 총액 및 추경 반영 시 실시간 예상 총액 표시
   - `원인행위액 (B)`: 원인행위 총액, 집행률(%) 및 프로그레스 바
   - `실제 지출액 (C)`: 출납 완료액 및 원인행위 대비 지출 전환율
   - `원인행위잔액 (A-B) & 불용 위험 모니터링`: 잔액 현황 및 집행률 70% 미만 사업 주의 배지

2. **3대 핵심 뷰/탭 구성**
   - **1. 세부사업 현황**: 사업별 예산 대비 집행률, 잔액 현황 시각화 및 세부사업별 소계 자동 계산
   - **2. 추경 시뮬레이터**:
     - 셀 단위 실시간 추경 금액 입력 (+/-) 및 방향별 시각적 색상 피드백 (증액 빨강 / 감액 파랑)
     - `원인행위잔액 전액 감액` 및 `추경 초기화` 원클릭 퀵 도구
     - 작업 취소/다시실행 (`Undo: Ctrl+Z / Redo: Ctrl+Y`) 지원
     - 추경 사유(비고) 작성 및 엑셀 내보내기(`xlsx`) 완벽 연동
   - **3. 세부지출현황**: 현금출납부 기반 개별 지출 건 상세 내역 조회, 월별/일별 필터링

3. **데이터 안전성 & 백업/복원**
   - 전체 작업 상태 `JSON 백업 파일 내보내기` 및 `복원(Import)` 지원
   - 데스크톱 런처(`run_app.py`) 하트비트 및 로컬스토리지 자동 영속화

4. **UI / UX & 인쇄 최적화**
   - 다크 모드 / 라이트 모드 완벽 전환
   - 컬럼 표시/숨김 맞춤 설정 (시트별 독립 저장)
   - A4 가로 규격 결재/보고용 인쇄 최적화 서식

---

## 📂 디렉토리 및 파일 구조

```
예산관리프로그램/
├── src/
│   ├── components/          # 모듈화된 UI 컴포넌트
│   │   ├── Header.jsx             # 상단 타이틀, 테마, JSON 백업/복원
│   │   ├── KPISummary.jsx         # 4대 핵심 재정 지표 요약 카드
│   │   ├── UploadSection.jsx      # 에듀파인 엑셀 2종 업로드 & 툴바
│   │   ├── SupplementToolbar.jsx  # 추경 퀵 프리셋 & Undo/Redo
│   │   ├── BudgetTable.jsx        # 세부사업 & 추경 통합 테이블
│   │   ├── ExpenseTable.jsx       # 세부지출현황 테이블
│   │   └── ColumnManager.jsx      # 컬럼 온/오프 맞춤 모달
│   ├── hooks/
│   │   └── useBudgetStore.js      # 전역 상태, 자동저장, 히스토리(Undo/Redo)
│   ├── utils/
│   │   ├── formatters.js          # 통화, 백분율, 날짜 포매터
│   │   ├── excelParser.js         # 사업관리카드 및 현금출납부 파싱
│   │   └── excelExporter.js       # 추경명세서/지출내역 엑셀 내보내기
│   ├── App.jsx              # 메인 대시보드 레이아웃
│   ├── index.css            # 글로벌 디자인 시스템 및 반응형/인쇄 스타일
│   └── main.jsx             # React 엔트리포인트
├── backup/                  # 격리된 레거시 백업 파일
│   └── legacy_html/         # 구버전 단일 HTML 백업 파일 보관
├── run_app.py               # 데스크톱 웹 뷰어 실행 런처 스크립트
├── viewer.html              # 독립형 단일 파일 뷰어
├── demo.html                # 데모 미리보기 파일
├── package.json             # 의존성 및 스크립트 설정
├── vite.config.js           # Vite 빌드 설정
└── GEMINI.md                # 본 프로젝트 가이드 문서
```

---

## 💻 실행 방법

### 1. 데스크톱 런처 실행 (추천)
```bash
python run_app.py
```

### 2. 독립형 뷰어 실행
- 브라우저에서 `viewer.html` 또는 `demo.html` 파일을 직접 열어 즉시 사용 가능합니다.
